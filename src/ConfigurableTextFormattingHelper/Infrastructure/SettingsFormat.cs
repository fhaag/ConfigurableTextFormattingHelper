namespace ConfigurableTextFormattingHelper.Infrastructure
{
	/// <summary>
	/// Represents a document format that may be used for storing settings.
	/// </summary>
	internal enum SettingsFormat
	{
		Unknown = 0,
		Yaml
	}
}
